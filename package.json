{
  "author": "Simon Repp",
  "browserslist": [
    "defaults"
  ],
  "dependencies": {
    "babel-loader": "^7.1.4",
    "babel-preset-env": "^1.7.0",
    "enojs": "^0.4.5",
    "http-server": "^0.11.1",
    "markdown-it": "^8.4.1",
    "webpack": "^4.9.1"
  },
  "description": "The eno-lang.org website",
  "devDependencies": {
    "webpack-cli": "^2.1.4"
  },
  "name": "eno-lang.org",
  "repository": "https://github.com/eno-lang/eno-lang.org",
  "scripts": {
    "build": "npm run js && npm run learn && webpack",
    "deploy": "node deploy.js",
    "js": "node src/enojs/generate.js",
    "learn": "node src/learn/generate.js",
    "release": "npm run build && npm run deploy",
    "serve": "http-server -a localhost -c-1",
    "watch": "webpack -w"
  }
}
